version: '3.8'

services:
  game-app:
    build: .
    container_name: 745269-docker-app
    ports:
      # 左边是外部访问端口，右边是容器内部端口，都是 1233
      - "1233:1233"
    volumes:
      # 把本地的 data 文件夹挂载到容器内，保证 SQLite 数据库文件永不丢失
      - ./data:/app/data
    restart: unless-stopped